sudo: required
services:
  - docker

#before_install:
 # - docker build -t gmatysik/northwind -f ./NorthwindMvc/Dockerfile.dev ./NorthwindMvc

#script:
#  - docker run stephengrider/react-test npm test -- --coverage

after_success:
    - docker build -t gmatysik/northwindmvc -f ./NorthwindMvc/Dockerfile.dev ./NorthwindMvc
    - docker build -t gmatysik/northwindservice -f ./NorthwindService/Dockerfile.dev ./NorthwindService

#  - docker build -t stephengrider/multi-client ./client
 # - docker build -t stephengrider/multi-nginx ./nginx
  #- docker build -t gmatysik/multi-server ./server
  #- docker build -t gmatysik/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push stephengrider/multi-client
  - docker push stephengrider/multi-nginx
  #- docker push stephengrider/multi-server
  #- docker push stephengrider/multi-worker
  